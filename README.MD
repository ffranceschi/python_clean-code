# Clean Code Python

```
python3 -m venv .venv
source .venv/bin/activate
```

## instalar os pacotes
```
pip install mypy
pip install pylint
pip install --upgrade pip
```
#### mypy -> para tipar os dados
#### pylint -> identacao

ou
```
pip install -r requirements.txt  
```